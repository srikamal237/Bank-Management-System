package Core;
public class Account {
    date: accountNumber, balance, customer (linked to Customer), transactionHistory

    Methods:
deposit(amount)
withdraw(amount)
transfer(toAccount, amount)
viewTransactionHistory()

}
